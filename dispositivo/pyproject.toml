[project]
name = "dispositivo"
version = "0.1.0"
description = ""
authors = [
    {name = "Ivan",email = "ivan_marvic@usal.es"}
]
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "requests (>=2.33.1,<3.0.0)",
    "opencv-python (>=4.13.0.92,<5.0.0.0)",
    "ultralytics (>=8.4.48,<9.0.0)"
]

[tool.poetry]
packages = [{include = "dispositivo", from = "src"}]

[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
